---
import Layout from '@/layouts/Layout.astro';
import ProductGrid from '@/components/shop/ProductGrid.astro';
import { useTranslations } from '@/i18n';
import { products } from '@/data/products';

const t = useTranslations('es');

const categories = ['Todos', 'Textil', 'Instrumento', 'Libro', 'Estrella'];
---

<Layout title="Tienda" description="Tienda Ponto Socorro">
  <section class="py-16 px-6">
    <div class="max-w-6xl mx-auto">
      <h1 class="font-serif text-4xl font-semibold text-primary mb-4">
        {t.nav.shop}
      </h1>
      <p class="text-secondary mb-12">
        Textiles, instrumentos y publicaciones de la comunidad
      </p>
      
      <ProductGrid products={products} />
    </div>
  </section>
</Layout>