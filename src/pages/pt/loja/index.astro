---
import Layout from '@/layouts/Layout.astro';
import ProductGrid from '@/components/shop/ProductGrid.astro';
import { useTranslations } from '@/i18n';
import { products } from '@/data/products';

const locale = 'pt';
const t = useTranslations(locale);
---

<Layout locale={locale} title="Loja" description="Loja Ponto Socorro">
  <section class="py-16 px-6">
    <div class="max-w-6xl mx-auto">
      <h1 class="font-serif text-4xl font-semibold text-primary mb-4">
        {t.nav.shop}
      </h1>
      <p class="text-secondary mb-12">
        Textiles, instrumentos e publicações da comunidade
      </p>
      
      <ProductGrid products={products} />
    </div>
  </section>
</Layout>